portfolio:
  release-id: dev
  jobs:
    worker-enabled: false
  storage:
    default-provider: LOCAL
    local:
      root: ${PORTFOLIO_LOCAL_STORAGE:../runtime/media}
  media:
    staging-cleanup:
      enabled: false
    cleanup:
      enabled: false
      cooling-period: 30d
